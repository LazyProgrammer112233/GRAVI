name: Deploy Supabase Edge Functions

on:
  push:
    branches:
      - main
    paths:
      - 'supabase/functions/**'
      - '.github/workflows/deploy-edge-functions.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Set Supabase secrets for v2 pipeline
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          supabase secrets set \
            ROBOFLOW_API_KEY="${{ secrets.ROBOFLOW_API_KEY }}" \
            HF_API_KEY="${{ secrets.HF_API_KEY }}" \
            --project-ref iwdxokuakjshsagazjvu

      - name: Deploy analyze-maps-url function
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          supabase functions deploy analyze-maps-url \
            --project-ref iwdxokuakjshsagazjvu \
            --no-verify-jwt

      - name: Deploy analyze-drive-folder function
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          supabase functions deploy analyze-drive-folder \
            --project-ref iwdxokuakjshsagazjvu \
            --no-verify-jwt

      - name: Deploy ai-analysis function
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          supabase functions deploy ai-analysis \
            --project-ref iwdxokuakjshsagazjvu \
            --no-verify-jwt

      - name: Deploy analyze-maps-url-v2 function
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          supabase functions deploy analyze-maps-url-v2 \
            --project-ref iwdxokuakjshsagazjvu \
            --no-verify-jwt
